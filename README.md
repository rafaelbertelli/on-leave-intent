# on-leave-intent
### A script to detect if user is leaving the page and call an action to be executed before leave.

--------------------------------

### installation
```terminal
npm install
```

### Testing
```terminal
npm run test:watch
```
